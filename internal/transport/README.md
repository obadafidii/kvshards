# Transport

This is the transport layer of the key-value store, it is responsible for handling the communication between the client and the server.

## HTTP



## Thoughts
I have this thought of building a `TUI` for this project, it will be a `CLI` tool that will allow you to interact with the key-value store.

I am also thinking of custom command called `SWITCH <transport>` it will allow you to switch between `transports` basically `http` and `ws`.
I don't even know if it is possible and then the use case.


I am not sure if this is a good idea, but I think it will be a good way to learn about `TUI` and `CLI` development.